import React from "react";
import {
  Box,
  Button,
  TextField,
  Typography,
  Tabs,
  Tab,
  Select,
  MenuItem,
  Table,
  TableHead,
  TableRow,
  TablePagination,
  TableCell,
  TableBody,
} from "@mui/material";

const ViewMarksheetInfo = (props) => {
  return <></>;
};

export default ViewMarksheetInfo;
